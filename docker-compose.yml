# Use postgres/example user/password credentials
version: "3.8"

services:
  db:
    image: postgres
    ports:
      - 5434:5432
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123 #unhackable
